import{createAfricaniesClient,createFetchTransport}from'../../dist/index.js';import{envelope}from'./fixtures.mjs';
const recorded=[];const fetch=async(url,init={})=>{recorded.push({url:String(url),method:init.method});if(String(url).includes('/file/generate'))return new Response(JSON.stringify(envelope([{file_name:'invoice.pdf',upload_url:'https://uploads.example.test/invoice?signature=redacted',s3_key:'documents/invoice.pdf'}])),{status:200,headers:{'content-type':'application/json'}});return new Response(null,{status:200})};
const transport=createFetchTransport({baseUrl:'https://api.example.test/api/v1',authorization:'Basic runtime-value',shipmentMode:'SFN',fetch});const client=createAfricaniesClient({transport});const uploaded=await client.files.upload(new Blob(['demo']),{extension:'pdf',mime_type:'application/pdf',folder:'documents'},{fetch});console.log(JSON.stringify({s3Key:uploaded.s3_key,requests:recorded.map(({method})=>method)}));
